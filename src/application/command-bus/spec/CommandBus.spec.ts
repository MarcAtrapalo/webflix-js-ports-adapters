import CommandBus from '../CommandBus';
import Command from '../Command';

describe('CommandBus', () => {

    it('should execute the appropriate CommandHandler');

    it('should do nothing if there is no CommandHandler for that command');

});
